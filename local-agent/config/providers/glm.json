{
  "id": "glm",
  "name": "GLM (Z.ai)",
  "baseUrl": "https://chat.z.ai/",
  "enabled": true,
  "selectors": {
    "chatInput": "textarea, div[contenteditable=\"true\"]",
    "sendButton": "button[type=\"submit\"], button[class*=\"send\"]",
    "stopButton": "button[class*=\"stop\"]",
    "assistantMessage": "[class*=\"assistant\"], .markdown-body, [class*=\"message\"][class*=\"bot\"]"
  },
  "completionRules": {
    "waitForStopButtonDisappear": true,
    "maxWaitMs": 300000,
    "_maxWaitNote": "Raised from 120s. A reasoning model on a plan- or build-sized prompt was still thinking when the old wait expired, so a partial response got extracted and then failed to parse. The wait ends as soon as the reply is complete, so a longer ceiling costs nothing on fast answers."
  },
  "_controlsNote": "Radix UI. Options carry data-value and data-selected. The casing of data-value is inconsistent in GLM's own markup - copy these values verbatim, do not derive them from the labels. Search / Advanced Search is not wired: its trigger was not captured.",
  "controls": [
    {
      "id": "model",
      "label": "Model",
      "kind": "select",
      "default": "glm-5.2",
      "options": [
        {
          "value": "glm-5.2",
          "label": "GLM-5.2"
        },
        {
          "value": "GLM-5.1",
          "label": "GLM-5.1"
        },
        {
          "value": "GLM-5-Turbo",
          "label": "GLM-5-Turbo"
        },
        {
          "value": "GLM-5v-Turbo",
          "label": "GLM-5V-Turbo"
        },
        {
          "value": "glm-4.7",
          "label": "GLM-4.7"
        }
      ]
    },
    {
      "id": "thinking",
      "label": "Deep Think",
      "kind": "select",
      "default": "Max",
      "options": [
        {
          "value": "High",
          "label": "High"
        },
        {
          "value": "Max",
          "label": "Max"
        }
      ]
    }
  ],
  "controlSelectors": {
    "modelTrigger": "button[aria-label=\"Select a model\"]",
    "modelOption": "[aria-label=\"model-item\"][data-value=\"{value}\"]",
    "thinkingTrigger": "[data-dropdown-menu-trigger]",
    "thinkingTriggerText": "Deep Think",
    "thinkingOption": "[data-selected]"
  },
  "profileDir": "local-agent/storage/browser-profiles/glm",
  "comingSoon": true,
  "_comingSoonReason": "Gated: the live site declines the build prompts, and the model and thinking controls were not found on the page. Selectors here have never been confirmed.",
  "contextBudgetChars": 150000,
  "_contextBudgetNote": "Roughly how much conversation this provider will hold before a build should continue in a new thread, in characters. A deliberately conservative guess - nothing can read the real window, and being wrong in this direction costs one seeded prompt while being wrong the other way loses a step fifteen deep. Counted from what we send and the replies we receive, never from the page."
}
